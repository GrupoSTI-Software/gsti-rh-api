/**
 * Nombre con el que nace la sucursal default de una empresa.
 *
 * Es solo el nombre inicial: el tenant puede renombrarla, y puede transferir
 * la marca de default a otra sucursal. Lo que no puede es quedarse sin ninguna.
 */
export const DEFAULT_BRANCH_OFFICE_NAME = 'Oficina principal'

/** Slug base de la sucursal default recién sembrada. */
export const DEFAULT_BRANCH_OFFICE_SLUG = 'oficina-principal'
