import { TENANT_UNSCOPED_REASON } from '#constants/tenant_unscoped_reason'

/** Motivo auditado para `TenantContext.runUnscoped` en sync biométrico cross-empresa (cron). */
export const ASSIST_SYNC_RUN_UNSCOPED_REASON = TENANT_UNSCOPED_REASON.ASSIST_SYNC_SCHEDULED
