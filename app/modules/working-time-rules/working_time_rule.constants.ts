/**
 * Constantes compartidas del módulo de reglas de jornada (`working_time_rules`).
 */

/** País por defecto de los topes de jornada (sistema mono-país hoy). */
export const DEFAULT_COUNTRY_CODE = 'MX'
